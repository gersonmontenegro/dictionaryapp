import { StyleSheet } from 'react-native';

export const SplashStyle = StyleSheet.create({
    SplashContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: "center"
    },
});